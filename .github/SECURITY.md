# Security Policy

Please do not report security vulnerabilities through public GitHub issues. Instead, contact the maintainer directly.

The library uses sensitive session cookies; ensure you never commit your `config/cookies.txt` or `config/user_agent.txt` files. They are ignored by default in `.gitignore`.
